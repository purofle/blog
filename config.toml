# The URL the site will be built for
base_url = "https://blog.akana.moe"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

generate_feeds = true

author = "日落果"

theme = "apollo"

taxonomies = [{ name = "tags", feed = true }]

title = "日落果的Blog"
description = "你好，欢迎来到日落果的Blog～"

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

[extra]
menu = [
  { name = "/home", url = "/", weight = 1 },
  { name = "/posts", url = "/posts", weight = 2 },
  { name = "/tags", url = "/tags", weight = 3 },
  { name = "/about", url = "/about", weight = 4 },
]
mathjax = true
mathjax_dollar_inline_enable = true

socials = [
  { name = "twitter", url = "https://twitter.com/purofle", icon = "twitter" },
  { name = "github", url = "https://github.com/purofle", icon = "github" },
  { name = "rss", url = "https://blog.archlinux.tech/atom.xml", icon = "rss" },
]

insert_anchor_links = "heading"

dynamic_note = true
